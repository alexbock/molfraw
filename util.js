"use strict";

// TODO complete this
function isLatexGreekLetterName(name) {
    var names = [
        "alpha", "beta", "gamma", "Delta", "delta", "omega"  
    ];
    return names.indexOf(name) !== -1;
}
